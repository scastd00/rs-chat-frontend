import React from 'react';
import { Card } from '@mui/material';

function ErrorChatCard(props) {
  return (
    <Card>
      Could not load the message.
    </Card>
  );
}

export default ErrorChatCard;
